# 2026-05-20 學習日記

## 今天做了什麼

- 回顧期末簡報進度：確認架構（6章、12-19張）、技術棧（Vite + GSAP + ScrollTrigger + Splitting.js）、核心定位（「Folio 是框架的成品」）都已在上次討論中定案
- 決定簡報風格：資訊密度選「圖文並進」（左視覺 + 右標題/bullets），動畫風格選「Apple / Cinematic」（字母飛入、全螢幕縮放過場、數字滾動、視差背景）
- 從零建立 Vite 簡報專案（`presentation/slides/`），安裝 gsap + splitting，手寫 `index.html`、`style.css`、`main.js`，7 個 section 骨架全部到位
- 排除 localhost 無法存取的問題：Vite 預設綁 IPv6 `[::1]`，用 `--host 127.0.0.1` 強制 IPv4 解決
- 根據使用者反饋修改：大標題從「這個專題有兩個主角」改成 `Folio`（Dancing Script 草書字體）；移除 AI 感過重的模板文字；配色從暗色系改成與 Folio 網站完全一致的白底
- 翻查 Folio 前端原始碼（`globals.css`、`page.tsx`），確認 Folio 實際配色：`#F4F3FF` 薰衣草白底、`#1E1B4B` 深靛藍文字、三色 blob 動畫（indigo/sky/violet）
- 引入 Dancing Script + Urbanist 字體（跟 Folio 開場動畫一模一樣），更新全部卡片改為白色半透明 glassmorphism 風格

## 明天要做的事

- [ ] 確認簡報在瀏覽器跑起來視覺正確（http://127.0.0.1:5173）
- [ ] 把 Folio 部署做完（Vercel 前端 + Render 後端），讓 Demo iframe 有真實網址可嵌入
- [ ] 開始填簡報各章節的真實截圖與內容（目前都是佔位）
