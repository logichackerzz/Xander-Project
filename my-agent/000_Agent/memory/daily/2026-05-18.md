# 2026-05-18 工作日誌

## 今日完成項目

### 1. 財經新聞 Tab 升級
- 市場情緒頁「財經新聞」Tab 從 Yahoo Finance RSS → 四來源輪替（CNBC / Investing.com / NPR / Google News TW）
- 前端 NewsTab 新增 summary 摘要、region badge（美股/台股/全球）、publisher
- 後端沿用 feedparser，已確認安裝

### 2. 字卡上期數據（prev）
- `CardData` 加 `prev` / `prevLabel` / `prevTone` 三個欄位
- `KpiCard` 在數字下方加一行小字，永遠佔位（invisible）確保高度一致
- **前端推算**（有 `change` 欄位）：SP500 / NASDAQ / Dow / VIX / US 10Y / DXY
- **後端補欄位**：
  - `macro.py` 重構 `_bls_latest` → `_bls_raw` + `_yoy` helper
  - CPI、失業率、ISM PMI 加 `prev_value` / `prev_period`
  - Fed 利率加 `prev_spread`

### 3. PPI 生產者物價指數加入
- 後端新增 `GET /api/macro/ppi`（BLS WPSFD49116，YoY，帶 `prev_value`）
- 前端總體經濟 Tab 加 PPI 卡，替換掉製造業就業月增幅（BLS proxy 意義較弱）
- 總體經濟排版：上排（10Y殖利率 / Fed利差 / DXY）、下排（CPI / PPI / 失業率）

### 4. BLS API Key 導入
- `macro.py`：有 key 走 v2（POST，500次/天），無 key 走 v1（GET，25次/天）
- `.env` 加 `BLS_API_KEY=3fc6bc2c0c8a45ba9ccc4e4730791f7d`（已驗證啟用）
- CPI 2.7%（上期 2.6%）/ PPI 4.4%（上期 3.7%）/ 失業率 4.3%（上期 4.3%）全部正常

### 5. 字卡載入動畫升級（三頁）
- 問題：`loading ? skeleton : value` 切換無過場，數字閃現
- 解法：`AnimatePresence mode="wait"` 包住條件渲染
  - 骨架：`exit={{ opacity:0, filter:"blur(4px)" }}`（0.18s 淡出）
  - 數字：`initial={{ opacity:0, y:6, filter:"blur(4px)" }} → animate={{ opacity:1, y:0, blur(0) }}`（0.4s）
  - Badge：`initial={{ scale:0.85 }}` delay 0.1s
- 套用頁面：`sentiment/page.tsx` / `macro/page.tsx` / `chart/page.tsx`

### 6. sessionStorage 快取預載系統
- 新建 `src/lib/dataCache.ts`：`readCache` / `writeCache`，TTL 2 分鐘，key 前綴 `folio:`
- 首頁（overview）：mount 後延遲 1.5s 背景靜默預載 18 個端點
- sentiment / macro `fetchOne`：先查 sessionStorage，命中直接顯示（無 loading 閃爍）
- 踩坑：首頁已有同名 `readCache` function → import 用 alias `readApiCache` / `writeApiCache`

### 7. 投信買賣超顏色修正
- 舊：台灣股市慣例（買超=紅，賣超=綠）→ 新：跟漲跌一致（買超=green，賣超=red）
- `sentiment.py` `get_tw_trust_net()`：移除舊台灣慣例注解，tone 邏輯對調

## 踩坑紀錄
- BLS v1 API 每 IP 每天 25 次，今日測試時打完 → 申請 key 解決
- `const API` 重複宣告：overview 已有 API 變數，新增時改用 `PREFETCH_BASE`
- `readCache` 函數名衝突：overview 自帶 local function，import 要 alias
- uvicorn 改完程式碼必須手動重啟（--reload 不可靠），投信顏色改了沒重啟導致仍顯示舊值

## 目前狀態
- 前後端均正常運行（port 8000 / 3000）
- BLS API key 已啟用，三張月度資料卡正常顯示
- 快取系統上線，首頁停留 1.5s 後開始預載，之後切頁即時顯示

## 下次待做
- [ ] 健康評分 Radar Tab（財報頁第 4 個 Tab）
- [ ] 同業比較 section（財報頁底部）
- [ ] 其他頁面套方案 A 配色升級
- [ ] **部署**：前端 → Vercel，後端 → Render（不是 Railway）

---

## 期末簡報討論（下午 session）

### 核心定位決定（重要）
> **「我設計了一套跟 Claude Code 協作的框架，而 Folio 只是這個框架的成品。」**

這是整個期末報告最核心的差異化論點。不是「我用 AI 做作業」，而是「我設計了一個讓 AI 能長期協作的系統，這個系統產出了 Folio」。

### 簡報大綱確認
| # | 段落 | 時間 |
|---|------|------|
| 0 | 開場 Hook：「這個專題有兩個主角」 | 1 min |
| 1 | 問題與動機 | 1-2 min |
| 2 | Live Demo（Folio） | 7-8 min |
| 3 | 技術架構 | 1-2 min |
| 4 | 代理型 AI vs 生成式 AI | 2-3 min |
| 5 | 我設計的協作系統（CLAUDE.md / 記憶 / Skills） | 2-3 min |
| 6 | 1+1>2 真實案例 | 1-2 min |
| 7 | 結語「協作不是依賴，是放大」 | 1 min |

### 未解決問題
- [ ] 1+1>2 的具體案例尚未系統性整理（本 session 沒有完整文件）
- [ ] 需要從 git log 和 daily log 回溯幾個最典型的案例

### 已建立文件
- 期末發表完整規劃 → `my-agent/100_Todo/projects/final-presentation.md`
- 長期記憶更新 → `memory/project_presentation.md`
