# 2026-04-25 工作紀錄

## Session 003 — 安全三件套 + GitHub 連結

### 安全設定
- `~/.bashrc` 新增 `trash()` 函式，`rm` 改為送進資源回收筒，`rm!` 為真正刪除
- `~/.claude/settings.json` 新增 `permissions.deny` 黑名單（20 條：rm -rf、sudo、dd、git reset --hard、git push --force 等）
- 權限模式設為 `acceptEdits`

### GitHub / Git 設定
- Git 身份：`logichackerzz` / `mybaby20050823@gmail.com`
- GitHub CLI：`gh` v2.91.0（winget 安裝）
- 登入方式：PAT token（`repo`、`workflow`、`read:org` 權限）
- `gh auth setup-git` 完成，日後 git push/pull 使用 gh 憑證

### 環境補充
- Shell：Git Bash（MSYS2），bash + cygpath 可用
- jq、npm、python、node 均不在 PATH，用 PowerShell 替代

### 結論
安全三件套完成，GitHub 連結完成。說「幫我推上去」即可推送至 GitHub。
