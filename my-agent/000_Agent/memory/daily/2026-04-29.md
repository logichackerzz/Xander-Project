# 2026-04-29 Session Log

## 完成事項

**財報頁面 — 財務健康 Tab**
- 後端 `/health` 端點：FCF（優先取 `Free Cash Flow` row，fallback 用 OCF - CapEx）、毛利率、D/E ratio
- 前端 `IncomeChart.tsx`：3 KPI 小卡（FCF 負值自動紅色 / 毛利率 / 負債比 + 白話標籤）
- FCF bar chart：每根柱子獨立上色（正 = 藍 `#3b82f6`，負 = 紅 `#ef4444`，用 recharts `Cell`）
- 毛利率 area chart（紫色 `#a855f7`）

**財報頁面 — 估值 Tab**
- 後端 `/valuation` 端點：P/E trailing/forward、P/S（key = `priceToSalesTrailing12Months`）、P/B、EV/EBITDA + 歷史 P/E 序列
- 歷史 P/E 計算：`ticker.quarterly_financials` 算 TTM Net Income → ÷ 股數 = TTM EPS → 季末股價（`ticker.history(period="3y")`）÷ TTM EPS
- 前端：3 KPI 小卡（P/E 附 Forward sub-line + 白話標籤）+ 全寬 P/E 歷史走勢圖
- P/E 歷史圖：amber 色 area chart，加均值 `ReferenceLine`（虛線 + label）

## 未完成 / 待辦

- [ ] 財報頁 UX 提升：使用者第一眼不知道要幹嘛，缺少引導感
- [ ] 資產頁補完：已實現損益彙總、圓餅圖、匯率換算
- [ ] 市場情緒頁（Fear & Greed、新聞流）
- [ ] 金融事件行事曆（名稱未定）：總經數據公告（CPI、Fed 利率決議、非農）、個股法說會時間——讓散戶掌握「今天有什麼大事會讓股價動」
- [ ] 財報頁動畫：對標 Apple 線上發布會的動畫質感（滾動進場、文字淡入、元素逐一飄出），讓「了解一間公司，從這裡開始」整頁有呼吸感——Week 6 用 Framer Motion 實作
- [ ] 估值 Tab：P/E 歷史最早 1-2 季數據可能偏高（TTM 資料不足），考慮加提示

## 關鍵決策

- yfinance P/S 的 key 是 `priceToSalesTrailing12Months`（不是 `TwelveMonths`）
- 歷史 P/E 用 3 年日線 + 季末日期比對，strip timezone 避免比較失敗
- 估值 Tab 不接 period selector（固定抓最近 8 季）
- uvicorn 新端點須手動重啟（Windows watchfiles --reload 不可靠）
