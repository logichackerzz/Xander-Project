# 2026-05-19 工作紀錄

## 完成項目
- 討論並決定部署方案：**Vercel（前端）+ Render（後端）**
- 釐清 Railway 無免費版（$5–8 USD/月），改選 Render
- 討論 Google Analytics 加入位置（`app/layout.tsx`，用 `<Script>` 元件）
- 在 `my-agent/100_Todo/projects/investor-dashboard.md` 新增完整部署待辦清單

## 關鍵決策
- 部署：Vercel + Render（不用 Cloudflare，Next.js SSR 相容性問題太多）
- Render 免費版冷啟動可接受，展示前 ping 一下喚醒即可
- SQLite vs PostgreSQL 未決定，明天部署前確認

## 明天待做
1. 申請 Vercel + Render 帳號（GitHub 登入）
2. 去 Google Analytics 建資源，拿 `G-XXXXXXXXXX`
3. 把 ID 給 Claude，讓他改 `layout.tsx`
4. 後端程式碼調整（PORT 環境變數、CORS、requirements.txt）
5. 部署上線 + 測試
