# 2026-04-27 工作紀錄

## 今天做了什麼

### 外部工具整合包
- 跑完完整訪談流程（Section A → E）
- 確認工具組合：Gmail、Firecrawl、GitHub（暫緩）、協作筆記空間

### 整合計畫文件
- 建立 `my-agent/100_Todo/integrations/2026-04-27-tool-integration-plan.md`
- 建立 `my-agent/000_Agent/knowledge/我的AI能幹清單.md`
- 建立 `my-agent/000_Agent/knowledge/我的工具清單.md`

### Firecrawl MCP 安裝
- 在 firecrawl.dev 申請帳號並取得 API key
- API key 存入 `C:\Xander-agent\.env`
- 建立 `.mcp.json` 設定 Firecrawl MCP server（npx firecrawl-mcp）
- `settings.local.json` 加入 `enableAllProjectMcpServers: true`
- ⚠️ 待驗證：重啟 Claude Code 後測試 Firecrawl 是否正常運作

### GitHub 推送
- 建立 `.gitignore`（擋掉 `.env`、`.mcp.json`、`settings.local.json`）
- 解除追蹤 `settings.local.json`（git rm --cached）
- Commit + push 到 `github.com/logichackerzz/Xander-Project`
- 35 個檔案、6225 行新增

## 安裝紀錄：/brainstorm Skill

- 來源：[lifehacker-tw/claude-code-mini-course](https://github.com/lifehacker-tw/claude-code-mini-course/blob/master/pro-kit/04-brainstorm.md)（by 雷蒙）
- Skill 位置：`my-agent/000_Agent/skills/brainstorm/SKILL.md`
- 計劃書存放：`my-agent/100_Todo/plans/`

**Xander 筆記理解**：`/brainstorm` 是 Plan Mode 加強版，強調在 AI 動手之前先透過問答釐清真正的需求、對齊方向，避免 AI 猜錯方向後做完才發現要砍掉重練。（不是「先建立結構再思考」，而是「先對齊需求再動手」）

## 待辦（上一 session 遺留）

- 重啟 Claude Code，測試 Firecrawl 是否正常載入（說「幫我抓這個網頁的重點：[網址]」）
- 協作筆記空間尚未正式建構（目前用現有 my-agent/ 資料夾，可以再細化）
- Gmail 整合尚未執行（需要 Google Cloud Console 申請 OAuth）
- 把過去得意的報告 / 作品丟進 `my-agent/200_Reference/past-work/`（上次遺留的作業）

---

## 散戶投資儀表板 — 專題規劃 Session

### 完成的事

**需求釐清（Brainstorm 式對話）**
- 確認使用情境：課堂展示 / 專題評分
- 確認技術棧：Next.js + Tailwind + shadcn/ui（前端）+ FastAPI（後端）
- 確認 MVP 優先：② 資產負債表（多市場）
- 確認持倉輸入：手動 + 圖片上傳（Claude API OCR 解析對帳單）
- 時程：6 週，截止約 2026-06-08
- 分工：Xander 下指令 + 測試，Claude 寫所有程式碼

**資料夾架構建立**
- `my-agent/100_Todo/projects/investor-dashboard.md`（計劃書、功能清單、時間表）
- `my-agent/400_Projects/web/investor-dashboard/`（完整專案骨架）
  - `backend/main.py`、`requirements.txt`、`.env.example`
  - `backend/routers/`：`portfolio.py`、`financials.py`、`sentiment.py`
  - `backend/scrapers/`、`services/`、`models/`（待填充）
  - `docs/api-spec.md`
- `MEMORY.md` 更新（記錄專題關鍵決策）
- 自動記憶系統新增 `project_investor_dashboard.md`

### 待辦（下次開始）

- [x] **Week 1 啟動**：確認 Node.js 已安裝，執行 `npx create-next-app` 初始化前端 ✅
- [x] 安裝 Python 套件：venv + 全套套件安裝完成 ✅
- [x] 測試 FastAPI 是否能啟動：`uvicorn main:app` → 回應正常 ✅
- [ ] 設計 UI 版型草稿（與 Claude 討論頁面佈局）

---

## Week 1 環境建置（2026-04-27 第二 session）

### 完成事項
- **後端**：Python venv 建立（Anaconda3 Python 3.12.7），安裝全套套件（FastAPI 0.115.0, pydantic 2.13.3, yfinance, anthropic, sqlalchemy, aiosqlite 等）
- **前端**：Next.js 初始化（TypeScript + Tailwind v4 + App Router + src/），shadcn/ui 裝好
- **驗證**：`GET http://localhost:8000/` 回傳 `{"status":"ok","message":"投資儀表板 API 運行中"}`，Next.js build 成功無錯誤
- **環境範本**：`.env.example`（後端）、`.env.local.example`（前端）已建立

### 踩坑紀錄
- `requirements.txt` 內有中文注釋 → pip cp950 編碼錯誤 → 移除注釋解決
- 分次安裝新舊 fastapi 導致 `_compat/` 目錄與 `_compat.py` 衝突 → 刪掉 venv 重建

### 下一步（Week 1 後半）
- [x] 設計 UI 版型草稿（側邊欄導覽 + 首頁儀表板佈局）✅
- [x] 建立 `frontend/src/app/layout.tsx` 主版型（深色主題 + 側邊欄）✅
- [ ] 確認 `.env.local` 設好後端 URL

---

## UI 骨架建置（2026-04-28 session）

### 完成事項
- **Sidebar**：`src/components/layout/Sidebar.tsx`，含 logo、4 頁導覽（總覽/持倉/財報/情緒）、active 高亮、深色主題
- **Header**：`src/components/layout/Header.tsx`，sticky top、含 slot 給頁面右側 action 按鈕
- **Root Layout 更新**：加入 Sidebar + `dark` class，title 改為「散戶投資儀表板」
- **4 個頁面骨架**：`/`（總覽）、`/portfolio`（持倉）、`/financials`（財報）、`/sentiment`（情緒）
- **TypeScript 0 錯誤**，`next dev` 可正常啟動

### 下一步（Week 2）
- [ ] `/portfolio` 持倉輸入 UI + TWSE/yfinance 報價串接
- [ ] 資產配置圓餅圖（Recharts 或 Chart.js）
- [ ] 確認 `.env.local` 設好後端 URL（`NEXT_PUBLIC_API_URL=http://localhost:8000`）

---

## Claude Code Statusline 建置（2026-04-27 第三 session）

### 完成事項
- 建立 `C:/Users/mybab/.claude/statusline-command.sh` 狀態列腳本
- 設定 `C:/Users/mybab/.claude/settings.json` 的 `statusLine` 區塊
- 診斷並修復三個主要問題：
  - `jq` 未安裝 → 改用 Node.js（`readFileSync(0, "utf8")`）解析 JSON
  - 進度條 `?????` → Unicode 字符在 Windows bash 不支援 → 改用 `#` / `-`
  - `remaining_percentage` 顯示 0% → 改為從 JSON 直接讀取（欄位確實存在）
- `/tmp` 在 bash vs node 路徑不同（bash→`AppData\Local\Temp`，node→`C:\tmp`）→ 用 fd 0 stdin 解決

### 狀態列最終顯示內容
`Sonnet 4.6 | [##----] | 13% | Cache:... | Ctx:87% | 5h:28% R:02:40 / 7d:18% R:07:00 | master* | +85/-2`

- **模型名稱**、**進度條**、**已用 %**、**Cache**、**Ctx 剩餘 %**
- **5h / 7d 配額** + **R:HH:MM** 重設時間
- **Git 分支** + dirty 標記 `*`、**diff 行數**
