# 2026-05-09 Session Log

## 完成項目

### 自選股頁全面改版
- 背景：淡紫白 → 中紫 `#E8E3FF → #DDE3FF`
- 卡片：深色 glass（`#1E1B4B/90`）+ 白字，在紫底上清晰浮出
- 搜尋框：Indigo 風格（`bg-white/80 border-indigo-200`、focus ring、白底下拉）
- 下拉選單 z-index bug 修正（search card 加 `relative z-20`）
- 標題改為「標的追蹤」

### 財報頁全面改版
- 所有元件（FinancialsSearch、CompanyCard、KpiCards、SummaryCard、IncomeChart）統一淺色 glass 風格
- Tab bar：`bg-muted` → `bg-slate-100/80`，active `bg-white`
- 數值顏色：`emerald-400` → `emerald-600`、`red-400` → `red-500`、`yellow-400` → `amber-600`
- Badge 背景：`bg-color-500/10` → `bg-color-50`
- Label 統一：`text-indigo-400 font-semibold tracking-widest`
- CompanyCard 改成 glass 卡片（有層次感）
- 大數字全補 `text-[#1E1B4B]`

### 跨頁轉場動畫（LayoutShell）
- 背景色 CSS transition 600ms（overview / watchlist / financials 各有對應色）
- Topbar 淡入淡出（opacity CSS transition 450ms）
- 頁面內容：`AnimatePresence mode="wait"`，`y: 14→0 + opacity`
- 移除 scale 動畫，改純 y 滑入（消除「縮回去」感）

### 財報頁搜尋流程動畫
- 首次搜尋：Hero 退場（`y -16 + opacity 0`）→ Skeleton 滑入（`y 16→0`）→ 資料淡入
- 外部跳入：`loading=true` 初始化，LayoutShell 帶整頁入場，無雙重動畫
- `AnimatePresence initial={false}` 防止外部跳入時重複觸發

## 待做（下次優先）
- [ ] IncomeChart Bar 圖風格改版（RevenueCard / FcfCard）
- [ ] 推上 Git
- [ ] 行事曆頁配色調整
- [ ] 市場情緒頁功能實作
- [ ] AI 白話解讀（Claude API）
