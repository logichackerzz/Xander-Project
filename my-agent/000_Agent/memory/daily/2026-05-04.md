# 2026-05-04 Session Log

## 完成事項

### 持倉頁 → 追蹤清單改版
- 持倉頁完全移除，重建為「追蹤清單」（Watchlist）
- 後端：`routers/watchlist.py`（GET / POST / DELETE `/api/watchlist`），SQLite `watchlist` table，`models.py` 新增 `WatchlistItem`
- 前端：追蹤清單頁有搜尋框（美股/台股切換 + `StockSearchInput`），選股直接加入，不用跳頁
- Sidebar「持倉管理」→「追蹤清單」，圖示換 Star

### 三頁連動
- 追蹤清單 → 點卡片 → `/financials?ticker=XXXX`（useSearchParams + Suspense）
- 財報頁 CompanyCard 右上角加 ★ 按鈕，加入/移除自動同步；市場判斷用 `currency === "TWD"` → tw，否則 us
- 行事曆頁原先加了自選股 ★ 高亮，後來使用者決定取消，已 revert

### 總覽頁大改版
- 後端：新增 `routers/market.py`（`/api/market/indices`，抓 ^TWII / ^GSPC / ^IXIC）
- 後端：`calendar.py` 新增 `/upcoming` 端點（14 天內行事曆事件）
- 前端：指數卡片（3 欄，顯示名稱 / 地區 tag / 價格 / 漲跌%）+ 行事曆事件列表
- **現況：總覽頁架構正常但內容空白**，原因是 uvicorn 尚未重啟載入新 market router，下次繼續處理

### UX 改善（全站）
- 刪除確認：`window.confirm` → `@base-ui/react/dialog` 自訂對話框
- 加入追蹤清單：成功後右下角 Toast（`useToast`）
- 財報頁底部移除 ✨ emoji
- 追蹤清單卡片「點擊查看財報 →」改為 hover 才顯示

### Bug 修復
- `HoldingsTable.tsx` 仍 import `GroupedHolding`/`Holding` → 加回 type export 到 portfolio/page.tsx
- 總覽頁 hydration 錯誤：`new Date()` 移入 `useEffect`
- `indices.map is not a function`：fetch 加 `Array.isArray` 防呆 + `r.ok` 檢查

## 未完成 / 下次待做
- [ ] 總覽頁指數資料空白：uvicorn 需重啟（`market` router 還沒載入），確認後應可正常顯示
- [ ] 財報搜尋框目前只搜美股（`market=us` 寫死），之後補台股支援
- [ ] 市場情緒頁（③ 模組）
- [ ] AI 白話解讀（Claude API，Week 6）
- [ ] Framer Motion 動畫（Week 6）

## 關鍵踩坑
- `Start-Process powershell` 從 bash 呼叫時 `$_` 變數有轉義問題，uvicorn 重啟要手動在後端視窗執行
- Next.js 16 hydration：render 階段任何 `Date.now()` / `new Date()` 都必須放在 `useEffect`
- TypeScript 物件 literal 多餘屬性（如 `dot`）即使有 `as any` 仍可能被 Next.js 編譯器拒絕，移除最乾淨
- `HoldingsTable` import type 來自 `@/app/portfolio/page`，改版時要保留 export
